spring.application.name=GymJournal

# Disable default Spring Security login page (we use Catalyst Auth)
spring.security.user.password=disabled

# Increase max file upload size for future report/photo uploads
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB
